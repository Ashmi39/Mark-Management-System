# Mark-Management-System
A web-based Mark Management System built using JSP, Servlets, JDBC, and MySQL.  It allows users to add, update, delete, and view student marks, and generate reports like top scorers and subject-wise results.
